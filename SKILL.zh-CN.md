---
name: large-task-orchestrator
description: 仅在用户显式调用 $large-task-orchestrator、选择“大型任务编排”或明确要求使用本技能时使用。将复杂需求经调查和必要决策整理为任务定义、验收、计划及可恢复状态，并自动衔接持久目标执行。普通复杂任务、状态询问及仅提及名称不自动触发。
---

# 大型任务编排

把初始需求转化为可实施、可验证的完整任务，并自动衔接执行。用户主动调用本技能即明确请求：条件成熟后为本次任务建立并开始持久目标，无需再次输入目标命令；用户本次明确要求仅分析、暂停或其他限制时以其要求为准。技能文件被搜索、审查或编辑不等于调用。

本技能负责需求收敛、任务契约、状态入口和目标接入。整体持续执行、局部阻塞处理、失败修复、压缩恢复及停止规则遵循适用的全局和项目 AGENTS.md，不另立一套规则。不存在用户自定义 AGENTS.md 时，仍遵循宿主指令与本技能已定义的任务契约，不要求安装作者的私人规则。按用户使用的语言维护任务状态和沟通。调用不扩大真实账户、费用、生产、删除、对外发送等操作授权，也不覆盖系统或工具限制。

## 1. 先调查，再判断缺口

先理解当前用户目标、对话背景、已确认决定和适用的 AGENTS.md；检查已有任务状态，判断是新任务还是恢复。按任务相关性查看项目结构、代码与已有实现、配置、测试、文档、项目约定及可直接读取的外部资料。先用目录和搜索定位，再读相关内容，不把全仓扫描变成启动门槛。

区分事实、用户决定、工程假设和未知项，并为关键结论保留来源。缺少整个任务描述时只询问实际需求，不生成虚构的任务或目标。

检查以下维度是否足以支持实施；不适用项说明原因，不要求用户逐项作答：

- 最终结果、现状与问题、必须实现的行为、可观察效果和不能改变的行为。
- 技术、业务、兼容性、性能、安全、数据和环境约束。
- 外部系统、真实账户及高影响操作的依赖和本次授权范围。
- 包含与不包含的范围，以及证明完成的方式。

## 2. 只收集必须由用户作出的决定

能够从调查得到或依照现有架构合理判断的内容自行决定。文件位置、命名、常规实现、错误处理、工具选择和合理验证属于工程判断。

只询问无法合理推断且会改变结果的产品行为、业务规则、重要兼容性或数据结构选择、授权边界及必须由用户提供的权限或信息。先核对本次已有决定与授权，避免重复询问；凭据通过现有安全授权入口提供，不写进任务文档。

把当前已知、彼此独立的必要问题尽量集中一次提出。每题简述为什么重要、影响范围、主要选项和差异，有合理推荐时说明理由。区分“必须答复后才能进行的步骤”与“采用默认假设即可继续的部分”；必要答复未到不能以默认值或超时替代。等待期间推进不依赖答复的调查和准备。新证据才引出后续问题，不串行盘问细枝末节。

## 3. 建立一个可恢复的任务入口

信息足够实施时，自动填充 [任务模板](assets/current-task-template.zh-CN.md)，不要把空模板交给用户。必要决定仍待答复时，也把已知定义及阻塞保存为草稿。

按以下顺序选择 `STATE`，在进度说明及目标中记录其绝对路径和专题标题：

1. 优先复用项目已有任务状态文件的本任务专题，补齐模板所需信息，保留其他专题。
2. 没有既定入口时，在实际工作项目根创建固定的 `当前大型任务.md`；英文任务可使用等价的 `CURRENT_TASK.md`。无项目时使用本次可写工作目录，不写入全局技能安装目录。
3. 后续持续更新同一个入口，不按阶段、重试或日期生成新任务文件。同一任务恢复时直接复用；开启新任务前核对旧状态，已完成内容按项目既有历史机制保留必要摘要。不得覆盖其他未完成任务；并行任务使用同一状态文件中独立专题或项目既定隔离工作区，并记录归属。

验收项使用稳定编号（如 A1）、可观察的通过条件、验证方法、证据和“已满足／未满足”状态。尚未验证也属于未满足，并标明原因；不能把计划执行当作证据。覆盖正常行为、必要错误路径、兼容性和相关回归，按风险选择测试、构建或实际运行，避免机械增加无关检查。

执行步骤数量随任务决定。每步记录状态（待处理／正在处理／已完成／被阻塞）、依赖、对应验收项和产出。模板标题可以适配现有文档，但所需信息不能丢失。

## 4. 自动接入持久目标并实施

确认调查足够、完整任务定义和验收及计划已形成、必要决策已答复且无阻止启动的真实阻塞后，立即按 [持久目标接入](references/persistent-goals.zh-CN.md) 检查运行时工具、建立或复用目标并读回确认，然后执行第一个未完成步骤。能独立推进的部分继续推进，不因部分路径阻塞而停下全部工作。

任务文档不是批准门槛。不询问是否开始、是否批准整个计划或是否进入下一阶段。用户明确“仅分析和方案”时只交付该范围的文档，不创建实施目标、不改实现。运行时限制优先，不通过本技能绕过只读或 Plan 模式。

## 5. 把实施事实写回任务契约

在阶段变化、重要决定或假设变化、发现问题或阻塞、验证后，以及压缩／交接前更新 `STATE`。保留最终目标、约束、验收编号、当前步骤、已完成、剩余、问题、阻塞和证据；当前内容保持简洁，原始长日志使用链接。步骤、依赖和验证方案可随事实修订，注明原因；只有涉及用户目标、重要约束、产品／业务决定或授权变化才重新询问，不静默降低验收要求。

恢复时读 `STATE`、适用 AGENTS.md 和最新用户补充；核对工作目录、分支／工作区改动及运行事实，再读取目标状态，从当前第一个可执行步骤继续。不要以状态文件中的旧完成标记替代已失效证据，也不要重复询问已记录且仍有效的决定。其他任务的改动保持其归属。

结束前用最初目标和最新用户决定复核 `STATE` 中需求、约束、逐项验收、剩余、阻塞和验证结果。测试失败的调查、修复、复验及尚可执行的必要工作按全局规则继续。全部必要验收有证据满足且整体目标完成，才按目标工具当前契约标记完成。

最终交付说明目标完成情况、关键修改、重要决定和假设、实际验证及关键验收结论、已知限制与遗留问题。被外部条件阻塞时报告已完成事实、受影响步骤、需要的输入与恢复位置，明确整体尚未完成，不用完成语气或“下一步可以”代替可执行工作。
